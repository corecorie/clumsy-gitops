apiVersion: apps/v1
kind: Deployment
metadata:
  name: clumsy:1.0.0-18
spec:
  replicas: 2
  selector:
    matchLabels:
      app: clumsy:1.0.0-18
  template:
    metadata:
      labels:
        app: clumsy:1.0.0-18
    spec:
      containers:
        - name: clumsy:1.0.0-18
          image: corex125/clumsy:1.0.0-18
          resources:
            limits:
              memory: "256Mi"
              cpu: "100m"
          ports:
            - containerPort: 8080
